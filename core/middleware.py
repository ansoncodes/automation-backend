"""
core/middleware.py

Custom Django middleware for the SMEC backend.

Currently a placeholder — middleware can be added here as the project grows
(e.g. request logging, API key header injection, maintenance mode).
"""
